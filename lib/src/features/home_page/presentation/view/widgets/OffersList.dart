import 'package:x_parts/src/core/utils/app_imports.dart';

class OffersList extends StatelessWidget {
  const OffersList({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 60.h,
      width: double.infinity,
      color: AppColors.darkGreyColor,
      child: HomeRowWidget(
        autScrollList: true,
        children: [
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.flashImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.saleImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.flashImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.saleImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.flashImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.saleImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.flashImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.saleImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.flashImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.carImage.svg()),
          ),
          Container(
            margin: EdgeInsets.symmetric(horizontal: 10.w),
            child: Center(child: Assets.test.saleImage.svg()),
          ),
        ],
      ),
    );
  }
}
